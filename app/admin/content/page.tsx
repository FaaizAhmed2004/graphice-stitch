import { redirect } from "next/navigation";
import Link from "next/link";
import { ArrowLeft, BookOpenText } from "lucide-react";
import { getSupabaseServerClient } from "@/lib/supabase/server";
import ContentManager from "./ContentManager";

export default async function ContentPage(){const supabase=await getSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/admin/login");const {data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="admin"&&profile?.role!=="staff")redirect("/portal");const [{data:posts},{data:portfolio},{data:faqs}] = await Promise.all([supabase.from("blog_posts").select("id,title,slug,excerpt,content,category,status,published_at,meta_title,meta_description").order("created_at",{ascending:false}),supabase.from("portfolio_items").select("id,title,description,category,image_url,published,featured,sort_order").order("sort_order"),supabase.from("faqs").select("id,question,answer,category,published,sort_order").order("sort_order")]);return <main className="min-h-screen bg-[#f3f2ee] text-[#171717]"><header className="border-b border-black/10 bg-[#171717] text-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-white/70 hover:text-white"><ArrowLeft className="h-4 w-4"/> Admin control room</Link><Link href="/admin/media" className="rounded-xl bg-[#d9ff53] px-4 py-2 text-sm font-black text-[#171717]">Upload work images</Link></div></header><div className="mx-auto max-w-7xl px-5 py-10"><div className="flex items-center gap-3"><BookOpenText className="h-6 w-6"/><div><p className="text-xs font-bold uppercase tracking-[.2em] text-black/45">Content operations</p><h1 className="mt-1 text-4xl font-black">Blog, portfolio & FAQs</h1></div></div><div className="mt-8"><ContentManager posts={posts??[]} portfolio={portfolio??[]} faqs={faqs??[]}/></div></div></main>}
