"use client";
import {useState} from "react";import {createSupabaseBrowserClient} from "@/lib/supabase/browser";
type Post={id:string;title:string;slug:string;excerpt:string|null;content:string;category:string|null;status:string;published_at:string|null;meta_title:string|null;meta_description:string|null};type Work={id:string;title:string;description:string|null;category:string;image_url:string|null;published:boolean;featured:boolean;sort_order:number};type Faq={id:string;question:string;answer:string;category:string|null;published:boolean;sort_order:number};
const input="admin-input";
export default function ContentManager({posts:initialPosts,portfolio:initialPortfolio,faqs:initialFaqs}:{posts:Post[];portfolio:Work[];faqs:Faq[]}){const db=createSupabaseBrowserClient();const [tab,setTab]=useState<"blog"|"portfolio"|"faq">("blog");const [posts,setPosts]=useState(initialPosts);const [portfolio,setPortfolio]=useState(initialPortfolio);const [faqs,setFaqs]=useState(initialFaqs);const [notice,setNotice]=useState("");const [post,setPost]=useState({title:"",slug:"",excerpt:"",content:"",category:"Digitizing Tips",status:"draft"});const [work,setWork]=useState({title:"",description:"",category:"Embroidery",image_url:"",published:true,featured:false});const [faq,setFaq]=useState({question:"",answer:"",category:"General",published:true});async function savePost(e:React.FormEvent){e.preventDefault();const p={...post,slug:post.slug||post.title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")};const {data,error}=await db.from("blog_posts").insert(p).select("id,title,slug,excerpt,content,category,status,published_at,meta_title,meta_description").single();if(error)return setNotice("Could not save blog post.");if(data)setPosts(v=>[data,...v]);setPost({title:"",slug:"",excerpt:"",content:"",category:"Digitizing Tips",status:"draft"});setNotice("Blog post saved.")}async function saveWork(e:React.FormEvent){e.preventDefault();const {data,error}=await db.from("portfolio_items").insert(work).select("id,title,description,category,image_url,published,featured,sort_order").single();if(error)return setNotice("Could not save portfolio item.");if(data)setPortfolio(v=>[...v,data]);setWork({title:"",description:"",category:"Embroidery",image_url:"",published:true,featured:false});setNotice("Portfolio item saved.")}async function saveFaq(e:React.FormEvent){e.preventDefault();const {data,error}=await db.from("faqs").insert(faq).select("id,question,answer,category,published,sort_order").single();if(error)return setNotice("Could not save FAQ.");if(data)setFaqs(v=>[...v,data]);setFaq({question:"",answer:"",category:"General",published:true});setNotice("FAQ saved.")}return <div><div className="flex flex-wrap gap-2">{(["blog","portfolio","faq"] as const).map(item=><button key={item} onClick={()=>setTab(item)} className={`rounded-xl px-4 py-2.5 text-sm font-bold capitalize ${tab===item?"bg-[#171717] text-white":"bg-white text-black/55"}`}>{item}</button>)}</div>{notice&&<p className="mt-4 rounded-xl bg-white px-4 py-3 text-sm font-bold">{notice}</p>}<div className="mt-5 grid gap-8 xl:grid-cols-[.75fr_1.25fr]">{tab==="blog"&&<><form onSubmit={savePost} className="rounded-2xl border border-black/10 bg-white p-6 space-y-3"><h2 className="text-xl font-black">New blog post</h2><input required placeholder="Title" value={post.title} onChange={e=>setPost({...post,title:e.target.value})} className={input}/><input placeholder="Slug" value={post.slug} onChange={e=>setPost({...post,slug:e.target.value})} className={input}/><input placeholder="Category" value={post.category} onChange={e=>setPost({...post,category:e.target.value})} className={input}/><textarea required placeholder="Excerpt" value={post.excerpt} onChange={e=>setPost({...post,excerpt:e.target.value})} className={input}/><textarea required rows={8} placeholder="Content" value={post.content} onChange={e=>setPost({...post,content:e.target.value})} className={input}/><select value={post.status} onChange={e=>setPost({...post,status:e.target.value})} className={input}><option value="draft">Draft</option><option value="published">Published</option><option value="scheduled">Scheduled</option></select><button className="w-full rounded-xl bg-[#171717] py-3 text-sm font-bold text-white">Save post</button></form><div className="space-y-3">{posts.map(item=><article key={item.id} className="rounded-2xl border border-black/10 bg-white p-5"><h3 className="font-black">{item.title}</h3><p className="mt-1 text-sm text-black/55">{item.status} · /blog/{item.slug}</p></article>)}</div></>}{tab==="portfolio"&&<><form onSubmit={saveWork} className="rounded-2xl border border-black/10 bg-white p-6 space-y-3"><h2 className="text-xl font-black">New sample design</h2><input required placeholder="Title" value={work.title} onChange={e=>setWork({...work,title:e.target.value})} className={input}/><input placeholder="Category" value={work.category} onChange={e=>setWork({...work,category:e.target.value})} className={input}/><input placeholder="Public image URL" value={work.image_url} onChange={e=>setWork({...work,image_url:e.target.value})} className={input}/><textarea placeholder="Description" value={work.description} onChange={e=>setWork({...work,description:e.target.value})} className={input}/><label className="flex gap-2 text-sm font-bold"><input type="checkbox" checked={work.published} onChange={e=>setWork({...work,published:e.target.checked})}/> Published</label><label className="flex gap-2 text-sm font-bold"><input type="checkbox" checked={work.featured} onChange={e=>setWork({...work,featured:e.target.checked})}/> Featured</label><button className="w-full rounded-xl bg-[#171717] py-3 text-sm font-bold text-white">Save sample</button></form><div className="space-y-3">{portfolio.map(item=><article key={item.id} className="rounded-2xl border border-black/10 bg-white p-5"><h3 className="font-black">{item.title}</h3><p className="mt-1 text-sm text-black/55">{item.category} · {item.published?"Published":"Hidden"}</p></article>)}</div></>}{tab==="faq"&&<><form onSubmit={saveFaq} className="rounded-2xl border border-black/10 bg-white p-6 space-y-3"><h2 className="text-xl font-black">New FAQ</h2><input required placeholder="Question" value={faq.question} onChange={e=>setFaq({...faq,question:e.target.value})} className={input}/><input placeholder="Category" value={faq.category} onChange={e=>setFaq({...faq,category:e.target.value})} className={input}/><textarea required rows={6} placeholder="Answer" value={faq.answer} onChange={e=>setFaq({...faq,answer:e.target.value})} className={input}/><label className="flex gap-2 text-sm font-bold"><input type="checkbox" checked={faq.published} onChange={e=>setFaq({...faq,published:e.target.checked})}/> Published</label><button className="w-full rounded-xl bg-[#171717] py-3 text-sm font-bold text-white">Save FAQ</button></form><div className="space-y-3">{faqs.map(item=><article key={item.id} className="rounded-2xl border border-black/10 bg-white p-5"><h3 className="font-black">{item.question}</h3><p className="mt-1 text-sm text-black/55">{item.answer}</p></article>)}</div></>}</div></div>}
